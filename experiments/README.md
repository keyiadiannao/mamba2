# 实验目录约定

- 命名：`{A|B|C|X}-{YYYYMMDD}-{short-name}/`
- 每个实验目录至少包含：
  - `README.md`：假设、复现命令、结果路径、`docs/experiments/EXPERIMENT_REGISTRY.md` 中的 id
  - 可选：`config.yaml`（或指向 `configs/` 中文件的相对路径）

模板可复制 `_template/`。
