# Pin versions after first successful install on both 5060 and AutoDL.
#
# Local (RTX 5060 sm_120): use cu128 wheels, NOT cu126:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# See environment/MAMBA2.md. AutoDL: pick wheel matching that GPU.
#
# torch>=2.2  # +cu128 on this machine
# numpy
# transformers>=4.40
datasets>=2.14
matplotlib>=3.7
# tqdm
# pyyaml
# Local minimal Mamba (no mamba-ssm on Windows): transformers>=4.45, accelerate
# mamba-ssm + causal-conv1d  # Linux/AutoDL for fused kernels — after torch matches CUDA
# pytest>=8  # optional: `python -m pytest tests/` (installed in mamba2 on dev machine)
